<Window x:Class="DispenserEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:DispenserEditor.Controls"
        xmlns:local="clr-namespace:DispenserEditor"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        mc:Ignorable="d"
        Title="Path Editor"
        MinHeight="600"
        MinWidth="1024">    
    <TabControl>
        <TabItem Header="Legacy Editor">
            <controls:PathEditorControl x:Name="Editor" Recipe="{Binding LegacyRecipe, Mode=TwoWay}" />
        </TabItem>
        <TabItem Header="Segment Editor">
            <controls:PathSegmentEditorControl x:Name="SegmentEditor" Recipe="{Binding SegmentRecipe, Mode=TwoWay}" />
        </TabItem>
    </TabControl>
</Window>
